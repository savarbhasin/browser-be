# Backend service configuration
API_PORT=8000
API_HOST=0.0.0.0
API_LOG_LEVEL=info
MODEL_PATH=/models/url_model.joblib

# CORS origins for the extension
CORS_ORIGINS=http://localhost:5173,chrome-extension://*

# Optional third-party keys (leave blank by default)
# TELEMETRY_API_KEY=
# ANTI_ABUSE_API_KEY=

# Instructions:
# - Do not commit real secrets.
# - In local dev, copy to .env and fill in as needed.
# - In CI, store as GitHub Secrets or Vault and inject at runtime.
